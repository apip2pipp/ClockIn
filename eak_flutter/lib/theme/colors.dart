import 'package:flutter/material.dart';

/// App Color Palette
/// Primary colors used throughout the ClockIn application

// Primary blue color (consistent across register, login, and onboarding screens)
const Color kPrimaryBlue = Color(0xFF26667F);

// Additional theme colors can be added here as needed
const Color kTextDark = Color(0xFF2D3748);
const Color kTextLight = Color(0xFF718096);
const Color kBackgroundLight = Color(0xFFF7FAFC);
